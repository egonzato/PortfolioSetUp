---
interface Props {
  listTitle: string;
  listItems: {
    title: string;
    journal: string;
    time: string;
  }[];
}
const {listTitle, listItems} = Astro.props
---

<h2 class='text-3xl font-bold mb-8 text-blue-900'>Recent Publications</h2>
		<div class='space-y-6'>
			{
				listItems.map(item => (
					<div class='p-6 rounded-lg bg-blue-900 text-white hover:bg-blue-800 hover:shadow-lg hover:-translate-y-1 transition duration-300 ease-in-out'>
						<h3 class='text-xl font-semibold mb-2'>{item.title}</h3>
						<p >
							{item.journal} • {item.time}
						</p>
					</div>
				))
			}
		</div>
